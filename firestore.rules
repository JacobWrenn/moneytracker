service cloud.firestore {
  match /databases/{database}/documents {

    match /{document=**} {
      allow update, delete, read: if owner();
      allow create: if true;      allow 
    }

    function owner(){
      return resource.data.user == request.auth.uid;
    }

  }
}